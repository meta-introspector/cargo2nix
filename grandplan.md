modify @submodules/cargo/Cargo.toml program to produces intelligent reports, respect .env files, disable all warnings, force all modules to build from submodules/ force all code to be checked into CRQ branch 016 for now, and enforce all our project guidelines. it should use nix for all bins. it should autoconfig. it should save all state and not rebuild. it should also be easty to augment and free the compiler state, we want a way to stop the compiler on errror, not exit but keep it alive, we can install a compiler hook to breakpoint to stop it on error or dump out the memory or set a ptrace or use linux ebpf/. we want to in rust cargo control the programs executed or inline them as libs. we can inline rust into cargo so it actulaly runs rust inside of cargo and we can stop on errors directly not as a sub process. .