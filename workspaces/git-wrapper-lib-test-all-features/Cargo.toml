[package]
name = "git-wrapper-lib-test-all-features"
version = "0.1.0"
edition = "2021"

[dependencies]
git-wrapper-lib = { path = "../../tools/git-wrapper-lib", features = ["git2", "with-anyhow", "with-walkdir", "with-regex", "with-toml_edit", "with-pathdiff", "with-serde", "with-serde_json"] }