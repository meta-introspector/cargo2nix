[package]
name = "git-wrapper-lib-test-git2-anyhow-serde"
version = "0.1.0"
edition = "2021"

[dependencies]
git-wrapper-lib = { path = "../../tools/git-wrapper-lib", default-features = false, features = ["git2", "with-anyhow", "with-serde", "with-serde_json"] }