name = "Export to Legacy Cargo.toml/Cargo.lock"
description = "Develop functionality to export the dependency graph and build configuration managed by the Nix ecosystem back into standard `Cargo.toml` and `Cargo.lock` files, allowing for interoperability with traditional Cargo workflows."
status = "todo"
depends_on = []