description = "Update `Cargo2NixCommand::needs_execution` to be more precise. It should return `true` if `Cargo.toml`, `Cargo.lock`, or any `.rs` file has changed *and* `Cargo.nix` is out of date."
steps = [
    "Modify `Cargo2NixCommand::needs_execution` to use Git state tracking for relevant files."
]