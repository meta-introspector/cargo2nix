# Task: Resolve Compiler messages for target: odds (Ticket ID: ee2c071c0855d44aacb4d65d4df47e05fdbce4e5e3db8c3e827a599747eb0b87)

**Primary Occurrence:**
- File: `submodules/odds/src/slice/rev.rs`
- Line: 98
- Message: warning: hiding a lifetime that's elided elsewhere is confusing
  --> submodules/odds/src/slice/rev.rs:98:17
   |
98 |     pub fn iter(&self) -> Rev<Iter<T>> {
   |                 ^^^^^         ^^^^^^^ the same lifetime is hidden here
   |                 |
   |                 the lifetime is elided here
   |
   = help: the same lifetime is referred to in inconsistent ways, making the signature confusing
   = note: `#[warn(mismatched_lifetime_syntaxes)]` on by default
help: use `'_` for type paths
   |
98 |     pub fn iter(&self) -> Rev<Iter<'_, T>> {
   |                                    +++



## Investigation Commands
```bash
rg --context 5 "hiding a lifetime that's elided elsewhere is confusing" submodules/odds/src/slice/rev.rs
```
```bash
rg --context 5 "hiding a lifetime that's elided elsewhere is confusing" submodules/odds/src/slice/rev.rs
```
```bash
rg --context 5 "hiding a lifetime that's elided elsewhere is confusing" submodules/odds/src/string.rs
```
```bash
rg --context 5 "hiding a lifetime that's elided elsewhere is confusing" submodules/odds/src/string.rs
```
```bash
rg --context 5 "hiding a lifetime that's elided elsewhere is confusing" submodules/odds/src/string.rs
```
```bash
rg --context 5 "hiding a lifetime that's elided elsewhere is confusing" submodules/odds/src/string.rs
```
```bash
rg --context 5 "hiding a lifetime that's elided elsewhere is confusing" submodules/odds/src/string.rs
```
```bash
rg --context 5 "hiding a lifetime that's elided elsewhere is confusing" submodules/odds/src/string.rs
```
```bash
rg --context 5 "hiding a lifetime that's elided elsewhere is confusing" submodules/odds/src/string.rs
```
```bash
rg --context 5 "hiding a lifetime that's elided elsewhere is confusing" submodules/odds/src/string.rs
```
```bash
rg --context 5 "hiding a lifetime that's elided elsewhere is confusing" submodules/odds/src/string.rs
```
```bash
rg --context 5 "hiding a lifetime that's elided elsewhere is confusing" submodules/odds/src/string.rs
```

