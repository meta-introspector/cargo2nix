# Task: Resolve Compiler messages for target: rustc_lint_defs (Ticket ID: 3a266638b5e50dbc338ff03d39276ef990e5ff6079b6521181a6a415a1e1e646)

**Primary Occurrence:**
- File: `submodules/rust/compiler/rustc_lint_defs/src/builtin.rs`
- Line: 2345
- Message: warning: unexpected `cfg` condition name: `bootstrap`
    --> submodules/rust/compiler/rustc_lint_defs/src/builtin.rs:2345:16
     |
2345 |     #[cfg_attr(bootstrap, doc = "```ignore")]
     |                ^^^^^^^^^
     |
     = help: expected names are: `docsrs`, `feature`, and `test` and 31 more
     = help: consider using a Cargo feature instead
     = help: or consider adding in `Cargo.toml` the `check-cfg` lint config for the lint:
              [lints.rust]
              unexpected_cfgs = { level = "warn", check-cfg = ['cfg(bootstrap)'] }
     = help: or consider adding `println!("cargo::rustc-check-cfg=cfg(bootstrap)");` to the top of the `build.rs`
     = note: see <https://doc.rust-lang.org/nightly/rustc/check-cfg/cargo-specifics.html> for more information about checking conditional configuration
     = note: `#[warn(unexpected_cfgs)]` on by default



## Investigation Commands
```bash
rg --context 5 "unexpected `cfg` condition name: `bootstrap`" submodules/rust/compiler/rustc_lint_defs/src/builtin.rs
```
```bash
rg --context 5 "bootstrap" submodules/rust/compiler/rustc_lint_defs/src/builtin.rs
```
```bash
rg --context 5 "bootstrap" Cargo.toml
```
```bash
rg --context 5 "bootstrap" build.rs
```
```bash
rg --context 5 "unexpected `cfg` condition name: `bootstrap`" submodules/rust/compiler/rustc_lint_defs/src/builtin.rs
```
```bash
rg --context 5 "bootstrap" submodules/rust/compiler/rustc_lint_defs/src/builtin.rs
```
```bash
rg --context 5 "bootstrap" Cargo.toml
```
```bash
rg --context 5 "bootstrap" build.rs
```
```bash
rg --context 5 "unexpected `cfg` condition name: `bootstrap`" submodules/rust/compiler/rustc_lint_defs/src/builtin.rs
```
```bash
rg --context 5 "bootstrap" submodules/rust/compiler/rustc_lint_defs/src/builtin.rs
```
```bash
rg --context 5 "bootstrap" Cargo.toml
```
```bash
rg --context 5 "bootstrap" build.rs
```
```bash
rg --context 5 "unexpected `cfg` condition name: `bootstrap`" submodules/rust/compiler/rustc_lint_defs/src/builtin.rs
```
```bash
rg --context 5 "bootstrap" submodules/rust/compiler/rustc_lint_defs/src/builtin.rs
```
```bash
rg --context 5 "bootstrap" Cargo.toml
```
```bash
rg --context 5 "bootstrap" build.rs
```

